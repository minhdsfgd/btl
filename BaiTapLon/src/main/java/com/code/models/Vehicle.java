package com.code.models;

public class Vehicle extends Item {

    public Vehicle(int itemId, int sellerId, String name, String description, double startingPrice) {
        super(itemId, sellerId, name, description, startingPrice);
    }
}
